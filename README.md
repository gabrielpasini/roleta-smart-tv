O projeto utiliza um banco de dados MongoDB para salvar os devs, para funcionar corretamente basta criar um arquivo .env na raiz do projeto e adicionar a seguinte propriedade nele:

```MONGO_DB=mongodb+srv://globoplay:smartapp@pasini.y8pg3.gcp.mongodb.net/globoplay?retryWrites=true&w=majority```
